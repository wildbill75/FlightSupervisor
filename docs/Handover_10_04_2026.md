# Handover - Agent du 10 Avril 2026

## 1. Ce qui a été accompli lors de la dernière session
*   **Fix Températures Cabine (Bug 17 / Bug 6)** : Le problème de surchauffe aléatoire au sol a été résolu en modifiant le mapping SimConnect (`SimConnectService.cs`) : l'application écoute désormais la variable d'environnement `AMBIENT TEMPERATURE` à la place de `TOTAL AIR TEMPERATURE` qui générait des fausses chaleurs au sol. Les tags dans le *Plan d'Action* ont été cochés `[x]`. 
*   **Bug 23 (Retard affiché de 23h)** : Finalement non reproduit par l'utilisateur. Le sujet est considéré comme clos.
*   **Workflow et Règles d'Organisation** : Une 12ème règle en lettres d'or a été ajoutée à `.agents/workflows/Organisation.md` insistant sur la **STRICTE INTERDICTION** de fouiller dans le code en arrière-plan sans consigne explicite de l'utilisateur.

## 2. Le défi immédiat pour ton tour (Les bugs restants du Plan d'Action)
Dès ton réveil, il faut proposer à l'utilisateur de traiter les points ci-dessous (Bugs identifiés dans le Plan d'action avec screenshot) :

*   **Bugs 19 & 20 - La logique inversée du Deboarding :**
    *   **Problème :** Lors du débarquement, le compteur part de 0 et monte à 164 (au lieu de 164 vers 0) et donne l'impression de relancer l'embarquement du Leg 1.
    *   **Piste laissée par moi-même :** Au moment où l'avion passe en phase de *Turnaround*, le backend déclenche *immédiatement* `LoadNextLeg()` (Maison d'exécution dans `MainWindow.xaml.cs`). Cette méthode remplace **instantanément** le manifeste des passagers par ceux du Vol suivant. Les nouveaux passagers arrivant non-embarqués (`IsBoarded = false`), le frontend affiche "0/164". Quand l'utilisateur clique sur "Débarquement", il ne se passe rien ou un comportement fallacieux s'installe. Il faudra différer la création du manifeste du Leg 2 ou purger le Leg 1 avant son remplacement.
*   **Bug 18 - Rapport de Vol Tronqué :**
    *   **Problème :** Le rapport ne récupère ni les données complètes du vol ni l'atterrissage.
*   **Bug 21 - Incohérences des Datas :**
    *   **Problème :** Incompréhension globale UI sur le suivi du Leg courant par rapport au plan de rotation entier.

## 3. Instructions formelles
**RÈGLE D'OR ABSOLUE : NE DÉMARRE AUCUNE MODIFICATION OU GDE RECHERCHE AVANT QUE L'UTILISATEUR NE T'INDIQUE LUI-MÊME QUELLE TÂCHE ABORDER.** Lis ce document, salue-le, rappelle-lui le menu des opérations du jour et attends ses consignes.

Bon courage !
